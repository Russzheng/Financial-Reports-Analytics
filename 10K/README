This folder contains a small sample of 10K filings and codes.

It is meant to clean the 10-K filings, applying LDA and double-checking LDA's performance.

Modify directories if data directories are different.

As commented in the code, GENSIM library is the python wrapper version of Java's mallet topic modeling library, same as what the paper used.

To run the program, simply: python3 <filename>.py.

clean_10K is for cleaning and preparing the data.

LDA_10K is for LDA model.
